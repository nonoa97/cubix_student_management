CREATE TABLE student (
                         id SERIAL PRIMARY KEY,
                         name VARCHAR(255) NOT NULL,
                         birthdate DATE,
                         semester INTEGER
);